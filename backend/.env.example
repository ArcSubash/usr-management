PORT=5000

# while copying the connection string, Don't use the SRV connection string.
MONGO_URI=your_mongodb_connection_string

# Any random string
JWT_SECRET=your_secret_key

ADMIN_EMAIL=admin@test.com
ADMIN_PASSWORD=admin123

# To enable real Nodemailer Email sending, generate an "App Password" on your Gmail account
# Go to Manage google account in your account -> Search App passwords -> Create password
# copy and paste the google account's gmail address and the 16 digit password below.
# This is used for secure user registration and authentication via OTP
SMTP_USER=your_gmail_address@gmail.com
SMTP_PASS=your_gmail_16_digit_app_password(with no space)


# And change this file's name to `.env`